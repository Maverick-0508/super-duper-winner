# Demo API Key Configuration
# This key is used for authentication in the MVP version
# In production, use a secure, randomly generated key

# Backend API key (server-side)
DEMO_API_KEY=demo-key-12345

# Frontend API key (client-side, must start with NEXT_PUBLIC_)
# Should match DEMO_API_KEY for demo user
NEXT_PUBLIC_DEMO_API_KEY=demo-key-12345
